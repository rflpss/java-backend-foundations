# 🌐 HTML5 Forms & Intermediate Structure

Study project focused on creating robust forms and exploring semantic HTML5 features.
*Course: ProgramadorBr - Web Development*

## 🚀 Key Concepts Learned

### 1. Advanced Forms (`<form>`)
* **Methods:** Understanding `GET` vs `POST`.
* **Attributes:** `action`, `name`, `id`.
* **Validation:** Using `required`, `min`, `max` attributes directly in HTML.

### 2. Input Types (The Evolution)
Moving beyond simple text inputs, explored modern HTML5 inputs:
* 🎨 **Visual:** `color`, `file`.
* 📅 **Time/Date:** `date`, `datetime-local`.
* 🔢 **Numeric:** `number`, `range` (slider).
* 📧 **Data:** `email`, `tel`, `password` (masked characters).

### 3. Other Form Elements
* **Selection:** `<radio>` buttons and `<checkbox>` logic.
* **Text:** `<textarea>` for multi-line input.
* **Dropdowns:** `<select>` and `<option>` lists.

### 4. Semantic HTML
* Using tags that give meaning to the code structure (improving SEO and Accessibility).

## 💻 Tech Stack
* **HTML5**
* **CSS3** (Internal styling for form layout)

---
*Developed by Rafael Lopes during the Fullstack journey.*